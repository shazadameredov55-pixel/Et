# AI Digital Product Production Agent — MVP dependencies
# All libraries below are free/open-source. No paid API is required to run
# the MVP. Optional paid providers (if the user configures one later) would
# be added as extra, clearly-documented dependencies — never a hard require.

pandas==2.2.2
openpyxl==3.1.5
Pillow==10.4.0
reportlab==4.2.2
matplotlib==3.9.2
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0
pytest==8.3.3
pytest-mock==3.14.0
PyYAML==6.0.2
# The Telegram bot (src/telegram_bot/) talks to the Bot API directly over
# HTTP via `requests` (see api_client.py) rather than using
# python-telegram-bot, keeping the dependency footprint smaller and every
# code path mockable/testable without a live network connection.
python-dotenv==1.0.1

# Similarity detection is pure-Python (see src/memory/similarity.py) —
# no embeddings API or ML library needed at this scale, so no extra
# dependency is added here.

# PDF integrity checks
pypdf==5.0.1
